import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Замените на ваш домен после деплоя
  site: 'https://avpdev.com',
  output: 'static', // Режим генерации статического сайта
});
