---
// src/pages/index.astro
// Отключаем пререндеринг, чтобы редирект происходил на уровне сервера (мгновенно),
// а не через HTML-страницу с надписью "Redirecting..."
export const prerender = false;

return Astro.redirect('/ru/', 301);
---